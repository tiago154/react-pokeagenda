type Pokemon {
  id: Int!
  name: String!
  url: String!
}

type ListPokemon {
  count: Int
  next: String
  previous: String
  pokemons: [Pokemon]
}

type Query {
  listPokemon(offset: Int, limit: Int): ListPokemon
}