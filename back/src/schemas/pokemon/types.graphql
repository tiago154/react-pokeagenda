type Pokemon {
  id: Int!
  name: String!
  image: String!
  types: [String!]!
}

type ListPokemon {
  count: Int
  pokemons: [Pokemon]
}

type Query {
  listPokemon(offset: Int, limit: Int): ListPokemon
}